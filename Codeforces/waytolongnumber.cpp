#include <iostream>
#include<string>
using namespace std;

int main() { 
    int n;
    cin>>n;
    cin.ignore();//this is most imp because it consumes the newline after reading n 

    for( int i=0;i<=n;++i){
       //ithe apan string call kartoy 
      string word;
      getline(cin,word);//here the code says it will read as the netire code 
      cout<<" word is "<<endl;
      }
      return 0;
    }
